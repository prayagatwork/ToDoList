<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>To-do</title>
  <script rel="stylesheet" href="css/styles.css"></script>
  <style>
    html {
      background: linear-gradient(300deg, #bdfbbe, #fcffb4, #c1caff);
      background-size: 180% 180%;
      animation: gradient-animation 3s ease infinite;
      min-height: 1000px;
      font-family: 'helvetica neue';
    }

    h1 {
      color: #fff;
      padding: 5px;
    }

    .box {
      max-width: 400px;
      margin: 50px auto;
      background: white;
      border-radius: 5px;
      box-shadow: 5px 5px 15px -5px rgba(0, 0, 0, 1);
    }

    .item {
      min-height: 70px;
      display: flex;
      align-items: center;
      border-bottom: 1px solid #F1F1F1;
    }

    .item:last-child {
      border-bottom: 0;
    }

    input:checked+p {
      text-decoration: line-through;
      text-decoration-color: #19178c;
    }

    input[type="checkbox"] {
      margin: 20px;
    }

    p {
      margin: 0;
      padding: 15px;
      font-size: 20px;
      font-weight: 200;
      color: #00204a;
    }

    form {
      text-align: center;
      margin-left: 20px;
    }

    button {
      min-height: 50px;
      width: 50px;
      border-radius: 50%;
      border-color: transparent;
      background-color: #19178c;
      color: #fff;
      font-size: 30px;
      padding-bottom: 6px;
      border-width: 0;
    }

    input[type="text"] {
      text-align: center;
      height: 60px;
      top: 10px;
      border: none;
      background: transparent;
      font-size: 20px;
      font-weight: 200;
      width: 313px;
    }

    input[type="text"]:focus {
      outline: none;
      box-shadow: inset 0 -3px 0 0 #19178c;
    }

    ::placeholder {
      color: grey;
      opacity: 1;
    }

    footer {
      color: white;
      color: rgba(0, 0, 0, 0.5);
      text-align: center;
    }

    #heading {
      background-color: #19178c;
      text-align: center;
      color: chartreuse;
      font-family: 'Courier New', Courier, monospace;
    }
  </style>
</head>

<body>

  <div class="box" id="heading">
    <h1>
      <%= listTitle %>
    </h1>
  </div>

  <div class="box">
    <% updatedItems.forEach(function(item){ %>
      <form action="/delete" method="post">
      <div class="item">
        <input type="checkbox" name="checkbox" value="<%=item._id%>" onchange="this.form.submit()">
        <p>
          <%= item.name %>
        </p>
      </div>
      <input type="hidden" name="listName" value="<%= listTitle %>"></input>
    </form>
      <% }); %>

        <form action="/" class="item" method="post">
          <input type="text" name="newItem" placeholder="Add Item">
          <button type="submit" name="list" value="<%= listTitle %>">+</button>
        </form>

  </div>

</body>

<footer>this is footer</footer>

</html>